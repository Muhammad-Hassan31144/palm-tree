# Load configurations in ProcMonProcessor
processor = ProcMonProcessor({
    "behavioral_filters_file": "config/procmon/behavioral_filters.json",
    "noise_filters_file": "config/procmon/noise_filters.json",
    "malware_patterns_file": "config/procmon/malware_patterns.json"
})

# The processor automatically applies:
# 1. Noise filtering (removes 85-95% of irrelevant events)
# 2. Behavioral pattern detection (identifies malware families)
# 3. Intelligent scoring (weighted threat detection)
# 4. Performance optimization (handles large logs efficiently)